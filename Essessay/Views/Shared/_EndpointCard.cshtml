@model DateTimeOffset

<div id="endpoint-result" class="card">
    <p class="text-sm font-medium text-gray-500 dark:text-gray-400">Response from <code class="rounded bg-gray-100 px-1 py-0.5 text-blue-600 dark:bg-gray-900">/api/endpoint</code></p>
    <p class="mt-1 text-lg font-semibold text-gray-900 dark:text-gray-100">Hello from Datastar 👋</p>
    <p class="mt-1 text-xs text-gray-400 dark:text-gray-500">Served at @Model.ToString("O")</p>
</div>
